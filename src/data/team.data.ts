import type { TeamMember } from '../types/team';

// Intentionally empty until the client approves real names, roles, bios and photos.
// The public team page presents verified service functions instead of invented profiles.
export const teamMembers: TeamMember[] = [];
